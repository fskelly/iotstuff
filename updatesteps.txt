export content to public folder
hugo -d public/

change to output folder
cd public/

Add new files
git add --all

git commit
git commit -m "<some change message>"

push changes
git push -f origin gh-pages
